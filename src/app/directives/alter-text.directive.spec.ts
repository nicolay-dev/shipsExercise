import { AlterTextDirective } from './alter-text.directive';

describe('AlterTextDirective', () => {
  it('should create an instance', () => {
    const directive = new AlterTextDirective();
    expect(directive).toBeTruthy();
  });
});
