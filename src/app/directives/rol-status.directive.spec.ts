import { RolStatusDirective } from './rol-status.directive';

describe('RolStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new RolStatusDirective();
    expect(directive).toBeTruthy();
  });
});
